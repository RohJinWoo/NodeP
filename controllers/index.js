const company = require('./company');
const sample = require('./sample');
const user = require('./user');
const pagenation = require('./pagenation');
module.exports = {
  company, sample, user, pagenation
};